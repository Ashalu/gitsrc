<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorAccent">@color/tadka_orange</item>
        <item name="drawerArrowStyle">@style/Toolbar.Arrow</item>

        <!-- you should set typeface which you want to override with TypefaceUtil -->
        <item name="android:typeface">serif</item>

    </style>

    <style name="Theme.Transparent" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>

    </style>

    <style name="AppThemeWithActionBar" parent="Theme.AppCompat">
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorPrimary">@color/Black</item>
        <item name="colorAccent">@android:color/white</item>
        <!-- you should set typeface which you want to override with TypefaceUtil -->
        <item name="android:typeface">serif</item>


    </style>

    <style name="AlertDialogCustom" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorAccent">@android:color/black</item>
        <item name="android:background">#FFFFFF</item>
        <!-- you should set typeface which you want to override with TypefaceUtil -->
        <item name="android:typeface">serif</item>
    </style>

    <style name="Toolbar.Arrow" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/tadka_green</item>
        <!-- you should set typeface which you want to override with TypefaceUtil -->
        <!--<item name="android:typeface">serif</item>-->
    </style>

    ///to change tab Header text size

    <style name="MyCustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">15dp</item>
        <!-- you should set typeface which you want to override with TypefaceUtil -->
        <!--<item name="android:typeface">serif</item>-->
    </style>

    <style name="TextsizeStyle" parent="@android:style/TextAppearance">
        <item name="android:textSize">15dp</item>
    </style>


    /// to change action bar title text size

    <style name="TitleTextStyle" parent="@android:style/TextAppearance">
        <item name="android:textSize">18dp</item>
        <!--<item name="android:typeface">serif</item>-->
    </style>

    <style name="ToolbarTitle" parent="Base.TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">16dp</item>
        <item name="android:textColor">@color/Black</item>
    </style>

    <style name="appBartextsize" parent="Base.TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">14dp</item>
        <item name="android:textColor">@color/Black</item>
    </style>

</resources>
